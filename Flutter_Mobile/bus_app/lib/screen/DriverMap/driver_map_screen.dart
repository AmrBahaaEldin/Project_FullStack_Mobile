

import 'package:flutter/material.dart';

class MapDriver extends StatelessWidget {
  const MapDriver({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          "MapDriver"
        ),
      ),
    );
  }
}
